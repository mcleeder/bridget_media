[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.11"
strict = true

[[tool.mypy.overrides]]
module = ["mpd.*", "feedparser.*", "apscheduler.*", "TP_lib.*"]
ignore_missing_imports = true
