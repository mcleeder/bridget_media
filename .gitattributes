# Files that run on the Pi must keep LF endings even when checked out on
# Windows (core.autocrlf would otherwise rewrite them to CRLF, and bash on
# the Pi chokes on \r).
*.sh      text eol=lf
*.service text eol=lf
*.conf    text eol=lf

# Fonts are binary
*.ttf binary
